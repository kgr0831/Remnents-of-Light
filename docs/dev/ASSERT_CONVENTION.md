# [ASSERT] 로그 규약

게임의 "검증 언어" 규약. `Debug.Log` 스팸이 아니라, 파싱 가능한 구조화된 로그로
플레이 테스트(STEP4의 `PlayTestRunner`) 및 사람이 함께 읽는다.

## 헬퍼

`Assets/Scripts/TestLog.cs` (정적 클래스, `#if UNITY_EDITOR || DEVELOPMENT_BUILD` 가드).

| 메서드 | 출력 형식 |
|---|---|
| `TestLog.Event(channel, msg)` | `[T=MM:SS.ff f=FRAME] [EVENT] channel: msg` |
| `TestLog.Assert(name, pass, detail="")` | `[T=MM:SS.ff f=FRAME] [ASSERT] name: PASS\|FAIL detail` |
| `TestLog.Step(scenario, step)` | `[T=MM:SS.ff f=FRAME] [STEP] scenario: step` |

타임코드 `[T=MM:SS.ff f=FRAME]`는 `Time.time`(분:초.센티초)과 `Time.frameCount`를 사용한다.

예:
```
[T=00:03.20 f=192] [EVENT] dash_iframe: dash_started
[T=00:03.30 f=198] [ASSERT] dash_iframe: PASS hits=0
[T=00:00.00 f=1] [STEP] dash_iframe: spawned
```

## 채널 네이밍 규칙

- 소문자 + 스네이크케이스 (`dash_iframe`, `combo_window`).
- 기능 단위로 채널을 나눈다 (하나의 전투 동작 = 하나의 채널).
- `Assert`의 `name`은 채널명과 동일하게 써서, 같은 채널의 `[EVENT]`/`[STEP]`/`[ASSERT]`를 로그에서 함께 필터링할 수 있게 한다.

## 현재 채널 목록 (다음 개발 작업 대응)

| 채널 | 검증 대상 |
|---|---|
| `dash_iframe` | 대시 무적 프레임 동안 피격 0회 |
| `dash_vfx` | 대시 잔상(산데비스탄 컬러 에코) VFX 녹화 showcase |
| `dash_afterimage_shape` | 대시 잔상(에코)의 실루엣이 플레이어 스프라이트와 일치(비율 오차 ≤0.01) |
| `respawn_fall` | 스폰보다 fallDeathDepth 아래로 낙사 시 스폰 복귀 |
| `wall_climb` | **⚠️ 진입 게이트(2026-08-04, 사용자 리포트 "플랫폼을 벽이라고 인식하고 벽타기가 된다")**: `wallLayer`는 Ground+Wall 통합 마스크인데 **실제 씬엔 Wall 레이어 콜라이더가 0개**(실측: 지형은 Ground 컴포지트 2개가 전부)라, 레이어로는 플랫폼과 벽을 구분할 수 없었다 — 모든 바닥 턱·타일 경계가 벽으로 인식됐다. 레벨 데이터를 바꾸는 대신 **기하(높이)로 판정**: 신규 `IsClimbableWall()`이 허리(0.5)·어깨(0.8)·정수리(1.0) 세 지점에 옆으로 레이를 쏴 **발밑부터 플레이어 키만큼 위까지 전부 막혀 있을 때만** 벽타기 진입을 허용한다(사용자 확정: 기준=플레이어 키). ⚠️ **진입 조건에만** 건다 — 유지/해제까지 걸면 벽 꼭대기에서 머리 위가 뚫리는 순간 `TryLedgeClimb`가 돌기 전에 떨어져 버린다. 키의 절반 미만 턱은 기존대로 걸어 올라가고(`TryStepUpShortWall`), 그 사이(절반~1키)는 벽타기도 걸어오르기도 아닌 "점프로 넘는 벽"이 된다. 검증은 `Tools/PlayTest/Wall Climb Gate` — **런타임 전용 테스트 지형**(씬 파일 무변경)을 그 자리에 세워 0.4키/0.7키/2.5키 세 면에 각각 붙여본다(실측 PASS: 0.4키=벽타기X·걸어올라감 max_y 0.75, 0.7키=둘 다 X, 2.5키=벽타기O). 테스트 설계 함정 2건: ① 바닥이 세 면을 다 덮지 않으면 플레이어가 낙사한다(1차 실행 y=-55) ② 최종 y로 걸어오르기를 판정하면 좁은 턱 위를 걸어 지나가 버려 오판한다 → **도달 최고 높이**로 잴 것 |
| `wall_climb` (이전 이력) | 벽타기(2026-08-03, 사용자 지시로 기존 Wall Slide 대체) — 벽 방향키를 누른 채 벽에 닿으면 붙는다(진입 조건만, `isTouchingWall`+`wallDirX` 방향 입력). **접지 여부 무관**(원래 `!isGrounded`를 요구해 땅에 서 있으면 진입 불가였음, 제거). **한 번 붙으면 방향키를 계속 안 눌러도 유지**(2026-08-03 2차 후속 지시) — `HandleWallSlide`가 "안 붙음"과 "이미 붙음" 두 분기로 나뉘어, 진입은 방향키가 필요하지만 유지는 `isTouchingWall`이 살아있는 한 방향 입력과 무관. **해제는 반대쪽 키 또는 Space**: 반대쪽 키(`Mathf.Sign(moveInput.x) == -wallDirX`) 또는 벽에서 물리적으로 떨어지면 즉시 해제, Space는 `HandleJump()`의 벽점프 분기(조건을 `isTouchingWall`+방향 입력에서 `isWallSliding` 하나로 단순화 — 방향키를 안 눌러도 붙어있기만 하면 작동)가 `wallJumpLockCounter`를 세팅하고 같은 프레임에 `HandleWallSlide`가 그 값을 보고 해제(호출 순서가 HandleJump→HandleWallSlide라 같은 프레임에 인계됨). 붙은 뒤엔 `moveInput.y`(W/S)로 `wallClimbSpeed`까지 위/아래 이동, 입력 없으면 `MoveTowards`로 속도 0에 수렴해 제자리 고정(자동 하강 없음). **붙어있는 동안 중력 완전 차단** — `rb.gravityScale`을 부착 순간 0으로, 이탈 순간 `Awake`에서 캐시해둔 원래값으로 복구, `isWallSliding`이 꺼지는 모든 경로(대시/차지/일섬/패링/처형/광원소모/반대키/Space)에서 함수 끝 한 곳에 모아 처리. 처음 붙는 프레임에 1회만 `SectionCamera.Shake(wallClimbShakeDuration, wallClimbShakeMagnitude)`. 스프라이트 미제작으로 애니메이터의 기존 "Wall Slide" 상태·`isWallSliding` 파라미터·`flipX` 로직 그대로 재사용 — 애셋 변경 0건. **벽 꼭대기 자동 오르기(2026-08-03 3차 후속 지시)**: `TryLedgeClimb()`이 매 프레임(벽타기 중일 때만) 머리 위(`b.max.y`+살짝, `ledgeWallCheckDist`≈0.15 짧은 거리)로 여전히 뭔가 있는지 확인 — 있으면 아직 꼭대기가 아니라 대기. 없으면 그 지점에서 조금 더 위(`ledgeProbeUpOffset`)·같은 x에서 아래로(`ledgeProbeDownDist`) 디딜 곳을 찾는다(벽 자체의 꼭대기든 별도 발판이든 `wallLayer`=Ground+Wall 통합 마스크로 다 잡음 — `groundLayer`로 좁히면 순수 Wall 레이어인 벽 꼭대기 자체를 못 찾는 함정이 있어 실측으로 확인 후 통합 마스크로 변경). 있으면 `isWallSliding=false`+중력 복구+`transform.position`을 그 자리 위로 순간 이동(이 프로젝트의 "즉시 이동" 컨벤션, 코루틴 보간 없음) — 수평 속도는 그 순간의 `moveInput.x`를 반영해 자연스럽게 이어짐. **애니메이션 버그 2건(2026-08-03 4차 후속 지시) 수정**: ① 접지 상태에서 벽타기가 풀려도(반대키 등) 애니메이터가 Wall Slide에 그대로 멈춰있던 문제 — 애니메이터 그래프엔 "Wall Slide→Idle/Run" 전이가 원래 없다(벽타기가 공중 전용이던 시절엔 뗄 때 항상 Jump/Fall(`isWallSliding IfNot` 조건 보유)나 Land 트리거를 거쳐 자연스럽게 빠져나갔는데, 접지 상태에서도 벽타기가 되게 바뀌면서 그 경로를 하나도 안 거치는 케이스가 생겼다 — AnyState 전이 목록을 리플렉션으로 실제 조회해 확인). 애니메이터 애셋을 새로 안 건드리고 `HandleWallSlide()`가 `isWallSliding`을 끄는 그 프레임에 `isGrounded`면 `anim.Play("Glitch Samurai-Run"/"Idle", 0, 0f)`로 직접 되돌림(`UpdateAlteredStateAnim`과 같은 패턴 — 그다음에 도는 `UpdateAnimations`가 폭주/초월 글리치 변형은 알아서 마저 처리). ② Wall Slide 클립이 W/S를 안 눌러도 계속 재생되던 문제 — `UpdateAnimations()`에서 `isWallSliding`이면 `anim.speed`를 `Mathf.Abs(moveInput.y)>0.01f`일 때만 정상 배율, 아니면 0으로(그 자리에서 클립 정지). **낮은 턱은 벽타기 대신 걸어 올라감(2026-08-03 5차 후속 지시, 스크린샷으로 지적)**: 신규 `TryStepUpShortWall()`이 벽 진입 직전에 먼저 확인 — 플레이어 허리 높이(`b.min.y + b.size.y*0.5f`)에 여전히 벽이 있으면 "진짜 벽"이라 정상 벽타기로 넘어가고, 뚫려있으면(턱이 반키보다 낮음) 그 위 디딜 곳을 찾아 곧장 그 자리로 옮기고 벽타기 진입 자체를 건너뜀(`HandleWallSlide`의 미부착 분기에서 `pushingIntoWall && !TryStepUpShortWall()`로 게이팅). **착지 높이 버그 발견·수정**: `TryStepUpShortWall`/`TryLedgeClimb` 둘 다 처음엔 `transform.position.y`를 콜라이더 중심으로 착각해 `착지면.y + b.extents.y`로 계산했는데, 실측해보니 이 프로젝트 플레이어는 피봇이 발밑(`transform.position.y == b.min.y`, `GetFloorY()` 등 다른 코드도 이미 이 가정)이라 반 캐릭터 키만큼 붕 뜨는 버그였다 — `feetOffset = transform.position.y - b.min.y`(보통 0)를 구해서 쓰는 방식으로 두 함수 다 수정. **6차 후속 지시 3건(2026-08-03) — 공중 벽타기 회귀·벽점프 우선순위·행동 입력 차단**: ① `TryStepUpShortWall()`의 "허리 높이" 기준선이 발밑 기준이라, 공중에서 벽 중간을 붙잡으면 그 순간의 발밑(=손이 닿은 임의 높이)부터 반 키 위까지를 재서 키가 큰 진짜 벽도 "위로 반 키만큼 안 남았다"고 오판 → 걸어 올라가기 시도가 실패하고 정상 벽타기 진입 자체가 막혀 그대로 떨어지는 회귀가 있었다(사용자 실측 "공중에서 벽타기가 안되는 문제와 떨어지는 문제") — `TryStepUpShortWall()`에 `if (!isGrounded) return false;`를 추가해 접지 상태에서만 동작하게 한정(공중은 항상 정상 벽타기로). ② `HandleJump()`가 `coyoteTimeCounter > 0f`(일반 점프)를 `isWallSliding`(벽점프)보다 먼저 검사해서, 접지 상태로 벽타기 중일 땐(coyoteTimeCounter가 접지라 항상 가득 참) Space가 일반 점프로 새 버렸다 — 벽타기의 "중력 0"은 안 풀린 채 일반 점프의 큰 상승 속도만 얹혀 사실상 중력 없이 치솟는 것처럼 보였다(사용자 실측 "매우 많이 올라가는 문제"). `isWallSliding` 검사를 `coyoteTimeCounter`보다 앞에 둬서 벽 부착 중엔 항상 벽점프(`wallJumpForce`, 정상 범위)가 우선하도록 순서 교체. ③ 벽타기 중엔 공격·E홀드·일섬·패링 입력 자체가 안 먹어야 한다(사용자 지시) — `OnAttack()`의 기존 "공중 입력 자체 차단"(`if (!isGrounded) return;`) 패턴에 `|| isWallSliding` 추가(입력 단계 차단), `HandleAttack()`의 스윙 시작 조건에도 `!isWallSliding` 이중 가드(기존에도 `isGrounded`가 입력 콜백과 실행 조건 양쪽에 있던 패턴과 동일). 일섬(홀드)·패링(탭)은 같은 `CanStartCharge()`를 공유해 거기 한 곳에 `&& !isWallSliding` 추가로 둘 다 봉인. E홀드는 `CanStartLightSpend()`에 직접 추가(`IsActionIdle`엔 안 넣음 — 넣으면 폭주/초월 진입 지연 조건까지 덩달아 바뀌어 이번 지시 범위(네 가지로 한정)를 벗어난다). **벽타기 속도도 이동속도 버프에 연동(2026-08-03 7차 후속 지시)**: 두 `targetY` 계산(이미 붙음/막 붙는 순간) 모두 `wallClimbSpeed * MoveSpeedMultiplier`로 변경 — 폭주·초월 중엔 벽타기 상하 이동 속도도 1.2배로 같이 빨라짐(`player_attack` 옆 신규 채널 참고할 것 없이 `MoveSpeedMultiplier`는 `PlayerController` 공용 프로퍼티). 리플렉션으로 500프레임치 `HandleWallSlide()` 반복 호출해 `rb.linearVelocity.y`가 정확히 3.6(=3×1.2)에 수렴함을 확인 |
| `dummy_damage` | 더미 몬스터 TakeDamage 시 HP 감소·피격 플래시·HP 0 사망 |
| `dummy_attack` | 더미 몬스터 추적→사거리 진입시 정지+창찌르기(windup/thrust/recover)→플레이어 피격, 공격 중 피격시 리셋. **피격 판정은 창끝 한 점(원)이 아니라 밑동(Windup 자세 위치)~창끝을 잇는 캡슐**(선분+반지름 hitRadius, `DummyEnemy.FindPlayerAtHitPoint`, 2026-08-02 확장, 사용자 지시 "창 전체가 범위") — 브로드페이즈 원 + `Collider2D.ClosestPoint` 정밀 판정, 터널링 방지 스윕도 선분-선분 거리로 확장 |
| `player_attack` | 플레이어 1-2타 콤보(Slash1/Slash2) 판정+데미지+히트스톱/카메라쉐이크 트리거. **히트박스 소스(2026-08-03)**: 기존 고정 오프셋+박스(`attackHitboxSize`/`attackHitboxDistance`) 대신, 사용자가 씬에서 Player 자식으로 배치한 비활성 BoxCollider2D 4개("1_R"/"1_L"/"2_R"/"2_L" — 1·2타 × 좌우)에서 `GetAttackHitbox(stage)`가 offset·size를 월드 변환해 읽는다(콜라이더는 물리에 안 낌, 위치·크기 데이터 전용). `FindParryTarget`의 1타 기준 박스도 같은 소스(`GetAttackHitbox(1)`) 공유. 자식을 못 찾으면(다른 씬 등) 기존 고정 필드로 폴백 + 1회 경고 |
| `ilseom` | 일섬 — 우클릭 2초 차지(취소/완료), 발동 시 무적·경로 판정(벽 1순위)·처형 피격 4배·20초 쿨타임. 에너지 부족(<40) 시 완충 확정 순간 미발동+쿨타임 미소모(`blocked_low_energy`), 쿨타임 중에도 짧은 탭=패링은 항상 통과(`blocked_cooldown`은 홀드 발동에만 적용) |
| `combo_window` | 3타 콤보 입력 윈도우 내 연결 |
| `dodge_counter` | 회피(대시)-카운터 — 대시 무적 유예(`dodgeCounterGraceWindow`) 중 적 찌르기가 닿으면 슬로우모션+확인키 대기로 진입. **`Tools/PlayTest/Dodge Counter x Time Accel`**(2026-08-04)은 "예비동작이 시작되는 순간(=`IsAttacking` 상승 엣지) 대시"라는 같은 플레이를 **가속 OFF/ON 두 번** 재현해 시간 가속이 이 판정을 깨지 않는지 본다. ⚠️ 테스트 설계 주의 2건(둘 다 1차 실행에서 기준선까지 FAIL로 실측): ① 기본 대시는 3.6유닛(20×0.18)을 날아가 창 사거리(2.28) **밖으로 빠져나가** 판정에 안 걸린다 → 이 테스트는 `dashSpeed`를 2로 낮춰 위치 변수를 제거하고 타이밍만 잰다(런타임 전용, 원복). ② `AttackTelegraphProgress ≥ 0`으로 기다리면 **진행 중이던 공격의 잔여 구간**에도 걸려 너무 이르게 눌린다 → `IsAttacking`의 상승 엣지를 기다릴 것. 실패 시 `took_hit`을 같이 남겨 "공격이 닿았는데 회피가 안 잡힘"(타이밍)과 "공격 자체가 안 닿음"(배치)을 한 줄로 구분한다 |
| `parry_timing` | 패링 — 우클릭 탭 판정(적 공격 캡슐 ↔ 1타 히트박스 겹침 — 2026-08-02부터 원이 아니라 밑동~창끝 캡슐, `dummy_attack`과 같은 기준), 성공 시 해당 공격 무효화 + 구형 실드 1회 방어 |
| `execution` | 처형 — HP 임계값(20%) 이하 적에 커서 호버 시 붉은 글로우 + "R/처형" 프롬프트, R 입력 시 무적·적 위치 이동·즉사 |
| `player_hud` | 체력 칸(갯수) + 빛 에너지 게이지 HUD — 맞은 칸이 Lerp로 꺼지고 켜진 칸 수가 정확히 1 줄어듦, 회복 시 다시 켜짐. 에너지 10% 이하 진입 시 게이지 색이 Lerp로 붉게 전환(회복 시 원복), 일섬 게이팅 실패 시 같은 장치로 1회 점멸. **광원바 색(2026-08-02, 픽셀 VFX와 통일)**: 평상시·초월 둘 다 흰색(`energyColor`/`transcendColor`), 폭주 중엔 붉은색(`rampageColor`) — `_rampageColorLerp`가 나머지 두 색 블렌드 위에 한 번 더 덧씌워진다(세 상태가 구조적으로 배타적이라 항상 하나만 활성). **변화량(고스트/예고) 색(2026-08-02, 후속 지시)**: `energyLossColor`=짙은 남보라, `energyGainColor`=밝은 금색 — 채움색이 흰색이든 붉은색이든 항상 뚜렷이 구분되도록 상태와 무관한 고정색으로 재조정(기존 연한 빨강/거의 흰색은 각각 붉은 채움·흰 채움과 겹쳐 안 보였다) |
| `player_damage` | 플레이어 피격 — 체력이 칸 단위로 깎이고 0 아래로 내려가지 않음. 피격 시 카메라 쉐이크 + 화면 붉은 점멸(알파 0→0.35→0) 후 베이스라인 원복, Canvas 오브젝트 누수 0 |
| `game_data` | 데이터 저장/불러오기 — 체력 칸·빛 에너지·체크포인트가 JSON으로 저장되고 불러오기로 복원 |
| `rampage` | 폭주 — **광원이 0이 되면 입력 없이 자동 진입**, **광원 25% 이상 회복해야 해제**(이력), 폭주 중 광원 획득 50% 감소, 다시 0이면 재진입. 폭주 중 피해 배율·넉백 면역·이동 감속 (2026-08-01 규칙 변경: 이전의 "Q 토글 + 초당 드레인 + 0에서 종료"는 폐기). **애니메이션(2026-08-02)**: 진입 중 공격·점프를 한 번도 안 하면 이동 애니가 안 나오던 버그 수정 — 히트스탑류가 걸 때마다 쓰던 `UnfreezeAnimAfter`가 `anim.enabled=true`만 하고 클립을 안 정해줘 마지막 프레임에 얼어 있었다, 이제 `anim.Play("Glitch Samurai-Idle", 0, 0f)`로 명시 복귀. 폭주·초월 중 지상 Idle/Run이 각각 `Glitch Samurai-Idle Gltich`/`Glitch Samurai-Run Gltich`로 자동 치환(`UpdateAlteredStateAnim`, 상태 진입 시 `normalizedTime` 보존), 해제 시 원래 클립으로 복귀. 점프 발동 순간엔 `HandleJump()`가 `Glitch Samurai-Jump Glitch`를 1회 강제 재생(Any State가 매프레임 되채워오는 Fall과 달리 Jump는 발동 시 1회성 클립이라 안전). **픽셀 VFX 색(2026-08-02)**: `PlayerController.CurrentPixelTint`가 폭주 중엔 `RampageBloomTint`(붉은색)를 반환해 광원 흡수·방출 픽셀이 전부 붉게 나옴(몸 마스크 블룸과 같은 색, `player_hud` 광원바도 동시에 붉은색). **이동속도 버프가 실제 속도·애니메이션 속도 양쪽에 반영(2026-08-03)**: `HandleMovement()`의 `moveSpeed`와 `UpdateAnimations()`의 `anim.speed`(공격 중이 아닐 때)가 공용 `MoveSpeedMultiplier` 프로퍼티(폭주 1.2/초월 1.2/평상시 1)를 공유 — 이전엔 `anim.speed`가 이동속도가 아니라 `AttackSpeedMultiplier`(1.4)를 잘못 재사용해 실제 이동속도와 애니 재생속도가 서로 다른 수치였다. 벽타기 속도 연동은 `wall_climb` 채널 참고 |
| `ego` | 자아 게이지 — 폭주 진입 시 100에서 시작해 초당 12씩 감소, 공격 적중 시 +15 회복, 폭주 해제 시 0으로 사라짐. HUD에 폭주 중에만 바가 나타남. **0이 되면 8초마다 체력 1칸 감소(`collapse_damage`), 자아가 다시 차면 타이머가 리셋되며 디버프 해제** |
| `rampage_vision` | 폭주 시야 제한 — 화면 모서리 암전 + 플레이어 중심 밝기 유지, 어둠 속 적의 붉은 링 + 어두운 코어, 적 컬링 반경(`CullRadius`=12유닛) 밖 적은 아웃라인 미생성, 지형 글리치 라인의 결손 집합이 스텝마다 재추첨됨, 해제 후 픽셀이 베이스라인과 완전 일치 + 오브젝트 누수 0. **지형 컬링 반경 분리(2026-08-03)**: `TerrainCullRadius`=7(적과 별도) — Map1처럼 방 하나가 카메라 한 화면(오쏘사이즈 8)에 딱 맞는 레벨에서 기존 12유닛 반경이 화면 밖 옆방 지형까지 끌어와 "동떨어진 조각"으로 보였다. **지형 소스를 Tilemap.HasTile() 셀 스캔 대신 CompositeCollider2D.GetPath()로 교체(같은 날, 더 근본적인 수정)**: Map1의 Base 레이어는 폭이 여러 셀인 타일을 듬성듬성 배치해 실제 충돌은 이어져 있는데 HasTile()은 "#...#...#..." 처럼 듬성듬성 true라, 채워진 셀 하나하나가 이웃 없는 것처럼 보여 따로 뜬 네모로 그려졌다 — 물리 엔진이 이미 갖고 있는 정확한 병합 폴리곤(`GetPath()`)을 직접 읽어 타일 배치 방식과 무관하게 실제 충돌 모양 그대로 나오게 함. ⚠️ `GetPath()`는 문서상 "로컬 좌표"이지만 이 프로젝트의 무회전 Base 콜라이더 기준 실측으로는 이미 월드 좌표였다 — `transform.TransformPoint()`를 추가로 씌우면 부모 체인 스케일(6.25배)이 중복 적용돼 171유닛까지 어긋남(raw 그대로 쓸 때 최소거리 1.5유닛 vs TransformPoint 적용 시 171유닛로 실측 확인 후 raw 사용으로 확정) |
| `trap_crumble` | 부서지는 바닥 — 밟으면 0.5초 경고 후 콜라이더가 꺼져 플레이어가 통과 낙하, 파편 생성, respawnDelay 후 복구 |
| `trap_press` | 압착기(프레스) — 주기적 상하 왕복, 내려찍을 때 아래에 깔린 플레이어가 피해+옆으로 넉백, 비켜서면 무해 |
| `door_switch` | 문/스위치 기믹 — 스위치(2026-08-10). `DoorSwitch`는 LightObject와 같은 원리로 Enemy 레이어에 둬서 물리는 통과하되 `PlayerController.CheckAttackHit`의 공격 판정엔 걸림 — 타격할 때마다 On/Off 토글(`IsOn`), `LaserDoor`가 매 프레임 폴링(이벤트 구독 없음 — 스위치 1개:문 여러 개 / 문 1개:스위치 여러 개(AND) 배선을 코드 변경 없이 지원). On이면 자식 Glow 오버레이(`Custom/PlayerBloomOverlay` 재사용)가 켜져 "1. main platforms.png"의 Switch 서브스프라이트 중 빨간 픽셀만(`Assets/Sprites/Gimmicks/SwitchEmissionMask.png` — 원본과 동일한 1024×768 크기로 구운 마스크, R 채널 대비 임계값 R>60 && R-G>30 && R-B>30으로 판별) 주황~노랑 사이 앰버색으로 빛남. `autoOffEnabled`+`autoOffDelay`(인스펙터 노출)로 On 상태 유지 시간이 지나면 자동 Off, 그 전에 재타격하면 타이머 취소. **레이어 분리(2026-08-10 후속)**: 처음엔 Enemy 레이어를 같이 썼는데, 폭주 중 "적만 어둠 속 붉은 링으로 보이고 나머진 암전"(`rampage_vision`) 로직과 충돌해 스위치가 진짜 적처럼 취급될 뻔했다 — 전용 `Switch`(18) 레이어를 새로 만들어 분리, `PlayerController.rb.excludeLayers`·공격 판정(`switchLayer` 전용 OverlapBoxAll, `enemyLayer`와 별도)에 각각 추가. **폭주 중에도 보임**: `RampageVisionFeature.ProtectedLayerMask`에 `Switch` 레이어 추가(원색 그대로 덧그려짐). **자아 게이지 제외**: `CheckAttackHit`에서 스위치 타격은 `hitCount`엔 잡혀 히트스톱·쉐이크는 그대로 느껴지지만 `restoreEgo` 플래그를 안 세워 자아 게이지는 안 채움(사용자 명시 지시) |
| `laser_door` | 문/스위치 기믹 — 레이저 문(2026-08-10). `Assets/MapAsset/DP_Set2_v1.0/_PNG/Animated/laser.png`(288×96)를 9프레임(32×96씩, `laser_0`~`laser_8`)으로 슬라이스(Unity 6 — `TextureImporter.spritesheet`는 제거됨, `UnityEditor.U2D.Sprites.SpriteDataProviderFactories` + `UnityEditor.SpriteRect`/`SpriteNameFileIdPair`로 슬라이스. 출처: https://docs.unity3d.com/6000.2/Documentation/Manual/sprite/sprite-editor/sprite-editor-data-provider-api.html, 실측으로 `SpriteRect`/`SpriteNameFileIdPair`가 문서의 `UnityEditor.U2D.Sprites` 네임스페이스가 아니라 `UnityEditor` 네임스페이스에 있음을 확인). `requiredSwitches` 전부 On이어야 안전(레이저 Off, 통과 가능) — 하나라도 Off면 위험(레이저 On), 스위치 미배선 시 항상 위험 취급. 점화 예비동작(`armingFrames` 0~2, 1회성) 후 완전 점등 루프(`loopFrames` 3~8 상당, 6프레임 반복)로 진입, 안전 전환 시 "3→0" 소등 애니메이션(`loopFrames[0]`→`armingFrames[2,1,0]`) 후 정지. **완전 점등 루프 중에만 피해**(예비동작·소등 구간은 텔레그래프로 무해, 사용자 확정) — 접촉 시 `PressTrap`과 동일한 `TakeDamage`+`ApplyKnockback` API 사용하되 ⚠️ **`pc.IsInvincible`을 확인하지 않는다** — 대시·일섬·처형 무적 프레임도 이 레이저는 무시하고 항상 피해(사용자가 PressTrap과 반대로 명시 지시). **막힘 처리(2026-08-10 후속, 사용자 리포트 "대시·걷기로 통과됨")**: 처음엔 트리거 콜라이더 하나로 피해만 줬는데, 트리거는 물리적으로 막지 못해 걷든 대시든 그냥 통과됐다 — `SetPhase`에서 완전 점등(Loop)일 때만 `col.isTrigger=false`(솔리드, 막음), 그 외(예고·소등·안전)엔 다시 트리거로 되돌린다. 솔리드일 땐 Trigger 콜백이 안 도니 `OnCollisionEnter2D`/`OnCollisionStay2D`도 같이 걸어 같은 `TryHitPlayer`로 처리(Phase.Loop 가드로 중복 안전). 플레이어 Rigidbody2D가 `CollisionDetectionMode2D.Continuous`라 대시(40) 속도로 부딪혀도 터널링 없음. **파란빛 글로우**: `Assets/Sprites/Gimmicks/LaserEmissionMask.png`(288×96, laser.png 전체와 동일 크기라 9프레임 UV 공유 — 임계값 B-R>25 && B-G>10으로 빔의 파란 픽셀만 추출)를 `DoorSwitch`와 같은 `Custom/PlayerBloomOverlay`로 적용, `sr.sprite`가 바뀔 때마다 `SetSprite()`로 글로우 스프라이트도 동기화. **전용 레이어(2026-08-10 후속)**: `Hazard`(17) 레이어 신설(Default는 하늘 배경 때문에 `BossOverlapOutlineFeature`에 못 넣음, Enemy는 `rb.excludeLayers`가 통째로 물리 통과시켜버려 못 씀) — `RampageVisionFeature.ProtectedLayerMask`에 추가해 폭주 중에도 보이고, `BossOverlapOutlineFeature.OutlineLayers`에도 추가해 보스 겹침 흰 아웃라인 적용. **보스 아웃라인이 폭주 중엔 안 보임(2026-08-10 후속)**: `BossOverlapOutlineFeature`가 `RampageVisionFeature`보다 렌더 순서상 나중이라 어둠 위에 흰 선이 그대로 뚫고 나왔다 — `AddRenderPasses`에 `RampageVisionFeature.Instance.Intensity > 0` 가드 추가해 폭주 중(페이드 포함)엔 패스 자체를 스킵, 레이저 문 아웃라인도 자동으로 이 규칙을 따름("폭주 제외") |
| `room_transition` | 방 전환 — `RoomTrigger`(BoxCollider2D 트리거) 진입 시 `SectionCamera.EnterRoom(bounds)`가 방 경계에 맞춰 카메라 위치·오쏘사이즈를 슬라이드(2026-08-03 부활, 이전엔 `RoomCamera`를 불렀지만 그 컴포넌트가 Main Camera에 없어 죽은 경로였다 — `SectionCamera`로 로직 이식, 룸 트리거 없는 씬은 기존 그리드 자동분할로 폴백). **방 크기(2026-08-03 후속 지시)**: `Room_A/B/C/D`(43~44×54, 4개) 대신 카메라 실제 값(`orthographicSize`×`aspect`)으로 정확히 한 화면 크기(28.44×16)를 계산해 맵 전체를 `Room_R{row}C{col}`(8열×4행, 32개) 그리드로 재배치 — 방 크기=화면 크기라 `EnterRoom`의 `roomTargetOrthoSize`가 항상 기존 카메라값(8)과 정확히 같아져 방 전환 시 줌 변화가 없다(위치만 슬라이드). `TiledMap_Exterior/CollisionTilemap`의 콜라이더 구성도 `TestFlatMap/Ground`와 동일하게 맞춤(`Rigidbody2D` Static + `CompositeCollider2D` Polygons/Synchronous + `TilemapCollider2D.usedByComposite=true`, 타일 경계 이음매 걸림 방지). **세이브 트리거는 아직 구현되지 않았다**(이전 설명이 아직 없는 기능을 적어둔 것으로 보임 — 실제로 이 채널이 검증하는 건 카메라 프레이밍뿐) |
| `light_pixel` | 광원 획득 시 픽셀이 적 주위 여러 방향에서 튀어나와 플레이어로 포물선 흡수, 도착마다 에너지가 나눠서 가산, 누수 0 |
| `light_spend` | 광원 소모(E 홀드) — 초당 -25 / 25당 체력 +1칸(만체력 시 실드 1개, 중복·리셋 없음) / 피격·10% 진입 시 즉시 중단 / 폭주 중 차단 / 종료 시 애니·카메라·VFX 원복. 지속 카메라 포커스(`SetSustainedFocus`)는 **X는 플레이어 중심으로 팬, Y는 원칙적으로 고정**하되(2026-08-02 1차 지시 — 이전엔 Y까지 완전 센터링돼 화면 위아래 프레이밍이 바뀌었다) **2차 지시로 완화**: `lightSpendCamPanDownMax`(1.2유닛) 한도 내에서 아래로만 팬 허용, 단 카메라 하단이 `GetFloorY()`(플레이어 발밑 레이캐스트) 아래로는 절대 못 내려감 — `floorLimit`과 `-maxPanDown` 중 바닥에 덜 가까운(0에 더 가까운) 쪽이 실제 한도. 실측: `maxPanDown`=1.2 허용에도 바닥이 먼저 걸려 `offsetY`=-0.55에서 멈추고 `camBottom`이 `floorY`와 오차 1e-5 이내로 일치 |
| `state_entry_defer` | 상태 진입 지연(T-1a, 2026-08-02) — 폭주·초월 공통. 처형·일섬·차지·대시·공격·회피카운터·패링·광원소모(`IsActionIdle`) 중이면 조건이 성립해도 진입을 미룸, 끝난 직후 진입. 예약 없음 — 대기 중 조건이 풀리면 진입 자체가 취소됨. 해제는 지연되지 않음(즉시) |
| `transcend` | 초월 — 광원 **100%**가 되면 입력 없이 자동 진입(`IsActionIdle` 필요), 진입 순간 주변 블룸 픽셀이 플레이어로 흡수되는 1회성 연출(`LightPixelFx.SpawnAbsorb`, 장식용 — 게이지 반영 없음) 직후 **릴리즈 비트**(`TranscendBurstFx`, PLAN §13 B안) — 흡수 도착 타이밍(0.45s 딜레이)에 맞춰 cyan-화이트 플래시 1회 + cyan 충격파 링 1회(0.25s) + 카메라 `FocusPulse` 단발(폭주의 절반 세기), 폭주(`RampageHeartbeatFx`)의 2박동·글리치·슬로우모와 의도적으로 대비되는 절제된 구성, 재생 완료 후 오브젝트 0개(카메라도 베이스라인 복귀). 유지 중 초당 **2.4** 드레인(정수 누적, `isExecuting` 중엔 드레인 없음, 2026-08-02 지시로 기존 6/5.0초의 2.5배인 12.5초로 연장), 광원 **70% 이하**로 내려가면 해제. 유지 중 상시 연출 — ~~cyan 아우라(`RampageAuraFx`)~~ **2026-08-04 사용자 지시로 제거**("아우라 느낌의 이상한 원 형태") — 폭주에서 두 번, 초월에서 한 번, 총 세 번 거절된 연출이니 되살리지 말 것. 현재는 몸통 둘레에서 위로 떠올라 사라지는 픽셀(`LightPixelFx.SpawnRiseOne`, 8개/초, 반경 0.6 스캐터, **색은 `CurrentPixelTint`(2026-08-02) — 초월 중이라 항상 흰색**, 광원 흡수/방출 픽셀·광원바 UI와 같은 규칙, 폭주 중엔 이 함수 자체가 안 불림). **스폰 기준점(2026-08-02)**: 가슴 피봇 대신 발밑(`transform.position` + `transcendPixelRiseYOffset`=0.1)에서 시작해 "더 아래에서부터" 떠오름 — 원형 스캐터가 아래 방향으로도 뻗어 바닥 밑까지 파고들 수 있어 `GetFloorY()` 레이캐스트로 스폰 Y를 바닥 아래로 못 내려가게 클램프(20회 스폰 샘플 최저 Y가 바닥과 오차 1e-5 이내로 일치 확인). 폭주와 같은 Jump/Idle/Run Glitch 애니 치환 공유(위 `rampage` 항목 참고). 초월 중 타격(+3)·패링(+25)·처형(+30)이 그대로 들어가 연장됨(획득 배율 없음). 일섬(-40)·광원소모(-25/s)로 70 아래 시 즉시 해제. 폭주와 동시 성립 불가(구조적 + 방어 가드). cyan 마스크 블룸(`Custom/PlayerMaskEmissive`, `_Color=(0.10,0.95,1.00)`, `_BloomBoost=3.5`) — 마스크 부위만 착색, 해제 0.25s 후 블룸·아우라·픽셀 전부 원복 + `PlayerBloomFx`/`RampageAuraFx` 오브젝트 0개 |
| `attack_telegraph` | 초월 중 적 미래 공격 범위 예고(T-3) — Windup 진입 시 `DummyEnemy.AttackTelegraphProgress`(0=예비동작 시작, 1=피해 확정 순간, 아니면 -1)가 매 프레임 갱신, 그 값으로 `AttackTelegraphFx`가 부착됨. **초월 중엔 `DummyEnemy.effectiveWindupDuration`이 `windupDuration`×`transcendWindupMultiplier`(2.5배, StartAttack 시점에 확정)로 늘어나 "공격 방향·범위를 더 일찍 확정짓고 보여준다"**(비초월 중엔 `windupDuration`과 완전히 동일, 회귀 없음). **모양은 원이 아니라 캡슐**(2026-08-02, 사용자 지시 "실제 공격 범위와 같게") — `AttackHitPointBase`(밑동)~`AttackHitPoint`(창끝) 선분 + 반지름(`AttackHitRadius`)을 절차 텍스처의 캡슐 SDF로 그림(`Custom/RampageOutline` 재사용, 신규 셰이더 없음), 중심·회전이 그 선분과 정확히 일치(오차 ≈0). 링 두께 월드 0.09(반지름 자체는 확대하지 않음 — 판정과 시각이 어긋나면 안 되므로 두께·발광 세기만 키움). 채움 스케일이 0→1로 단조 증가(캡슐 전체가 실제 크기로 구워져 있어 스케일=1이 곧 풀 크기). 패링·회피·적 피격으로 무효화(`LastAttackNeutralized`)되면 터짐 없이 0.12s 페이드 / 피해 확정·빗나감이면 0.10s 동안 1.0→1.25배 팽창 후 페이드. **정렬 순서 15**(2026-08-02, 사용자 지시 "플레이어 위에 표시" — 적 sortingOrder(5)+1이면 플레이어(10)에 가려졌다, `LightPixelFx`가 쓰는 "플레이어보다 앞" 고정값과 동일). 컬링 반경 **12유닛**(`TranscendVisionFx`, 0.25s 스캔 + 매프레임 판정) 밖 미생성, 초월 아닐 때 0개, 해제 시 예고·드라이버 오브젝트 전부 0개(누수 없음) |
| `time_accel` | 시간 가속(2026-08-04) — **Left Alt 토글**로 켜고 끈다(`KeyPressedThisFrame(Key.LeftAlt)` 직접 폴링, 처형 R·폭주 Q·광원소모 E와 같은 컨벤션이라 `.inputactions` 변경 0건). ⚠️ **1차 구현은 Shift 탭=대시/홀드=가속이었는데 대시가 "뗄 때" 나가게 되면서 조작감이 깨져 사용자가 회귀 리포트** → 키를 분리하고 홀드 대신 토글로 확정, 대시는 누른 즉시 발동으로 원복 + `dashInputBuffer`(0.12s) 신설(쿨타임 잔여·공격 끝자락에 눌린 입력을 버리지 않고 조건이 열리는 첫 프레임에 발동). (진입 시 광원 20% 획득·초월 블룸은 넣었다가 같은 날 사용자 지시로 둘 다 제거 — 진입 보너스는 토글할 때마다 들어가 켰다 껐다 반복하면 광원을 무한히 벌 수 있는 구멍이었다. 소모속도 2배는 유지.) ⚠️ **`dodgeCounterGraceTimer`만은 플레이어 보정에서 제외**한다 — 이건 "내 동작의 길이"가 아니라 "적의 공격 타임라인과 겹치는가"를 재는 판정 창이라 세계 시간으로 세야 관계가 유지된다. 실시간으로 세면 가속 중 예비동작이 실시간 0.625s(0.25/0.4)인데 유예는 0.35s라 창이 열리기 전에 만료돼 **대시-카운터가 통째로 안 터진다**(사용자 리포트, `dodge_counter` 채널 대조 실험으로 증명: 유예를 옛 실효값 0.14로 낮추면 `took_hit=True countered=False`로 같은 증상 재현, 0.35면 가속 ON/OFF 둘 다 발동). 가속 중 세계는 `Time.timeScale`=`timeAccelTimeScale`(0.4, 사용자 지정)로 통째로 느려지고 **플레이어만** `TimeAccelMul`(=1/0.4=2.5)로 되돌려져 실시간 이동거리·애니 재생·타이머가 평상시와 동일하다(속도 ×mul, 중력·가속도 ×mul², `Time.fixedDeltaTime`도 같은 배율로 줄여 스텝당 이동량과 물리 실시간 주파수(50Hz)를 유지 → 터널링·20Hz 끊김 방지). `Physics2D.maxTranslationSpeed`(기본 100)도 같은 배율로 올린다 — 이 프로젝트에선 단순 안전장치가 아니라 **실질 종단속도**다(낙하 가속 159u/s²라 0.63초면 도달, 실측). 안 올리면 보정된 속도가 여기 잘려 가속 중 낙하만 1/mul로 느려진다. 광원은 **실시간** 초당 `transcendDrainPerSecond`×`timeAccelDrainMultiplier`(3 = 초월의 1.5배 × 후속 지시 2배)=**7.2** 소모(느려진 세계와 무관), `timeAccelMinEnergyPercent`(10%) 이하가 되면 강제 해제 + 그 위로 회복할 때까지 재진입 불가. 폭주·회피카운터·일섬·처형·광원소모 중엔 진입 불가(진행 중이면 자동 해제 — 그 시퀀스들이 자체 timeScale/연출 타이밍을 소유). 연출은 회피-카운터와 동일(흑백 확산 `dodgeGrayscale*` + 하늘색 틴트 `dodgeCounterGlowColor` + 액션 블룸 + 진입 쉐이크 + 포커스 펄스 1회, 확인키 UI인 `DodgeUI`만 제외). 잔상은 가속·회피카운터 양쪽 모두 수명이 `fastAfterImageFadeMultiplier`(2배)만큼 짧아짐(일반 대시 잔상은 그대로), 가속 중 스폰 간격은 별도 `timeAccelAfterImageInterval`(0.05s — 대시의 0.01s를 수 초간 쓰면 초당 100개가 쌓인다). 종료 경로가 무엇이든(뗌·광원고갈·상태잠금·`OnDisable`) `Time.timeScale`·`Time.fixedDeltaTime`·중력·스프라이트 색이 베이스라인으로 복원 |

새 전투 동작을 추가할 때는 이 표에 채널을 먼저 추가하고, `PlayTestRunner` 시나리오와 짝을 맞춘다.
(`.claude/skills/add-combat-move/SKILL.md` 참고 — STEP6)
