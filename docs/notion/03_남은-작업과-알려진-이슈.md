# 🚧 남은 작업 & 알려진 이슈

> 기준일 **2026-08-04**
> 우선순위 — 🔴 지금 막고 있음 · 🟠 곧 필요 · 🟡 여유 있음 · ⚪ 언젠가

---

## 🔴 지금 진행을 막고 있는 것

| # | 항목 | 상세 |
| --- | --- | --- |
| 1 | **Map1에 Wall 콜라이더 미배치** | 벽타기가 명시적 `Wall` 레이어 콜라이더에만 붙도록 바뀌었는데, Map1에 아직 하나도 없다. **배치 전까지 벽타기 기능이 아예 발동하지 않는다.** `Tools/Level/Create Wall Collider` (Ctrl+Shift+W)로 배치 → 씬 저장은 사용자가 직접 |
| 2 | **미커밋 변경 213개 / 마지막 커밋 07-25** | 07-26 이후 **10일치 작업 전부가 워킹 트리에만 있다.** 광원·폭주·자아·초월·시간 가속·벽타기·맵 전환이 전부 여기 포함. 커밋은 승인 필요 작업이라 계속 미뤄져 왔다 |

---

## 🟠 곧 필요한 것

### 검증 인프라

- **`PlayTestRunner` 좌표 재보정** — 맵이 `Map1`으로 바뀌면서 기존 시나리오의 하드코딩 좌표가 어긋났다.
  (평지 좌표 `(10, 24)`가 현재 유효한 기준점)
- **미작성 시나리오 3종** — `state_entry_defer` · `transcend` · `attack_telegraph`.
  지금은 리플렉션 직접 검증으로 대체돼 있다.
- **`InputInjector`의 마우스 버튼 경로 미검증** — `PressButton`(Attack/Parry)이 키보드에서 겪은
  비트필드 문제와 같은 결함을 가지는지 아직 확인 안 됨.

### 삭제 후보 (전부 **승인 필요**)

| 파일 / 필드 | 상태 |
| --- | --- |
| `Assets/Scripts/RoomCamera.cs` | 무참조. `SectionCamera`로 완전히 대체됨 |
| `Assets/Scripts/VFX/RampageAuraFx.cs` | 호출부 0개. 아우라 연출이 **총 세 번 거절**되어 제거됨 |
| `PlayerController.rampageDrainPerSecond` | 고아 필드(옛 지속 드레인 방식의 잔재) |
| Animator의 미사용 상태 | Glitch/Slash/Death 계열 다수 |

---

## 🟡 계획 대비 밀린 콘텐츠

### Week 2 잔여 (타일맵 · 기믹)

- 패럴랙스(Parallax) 2.5D 배경 — 미착수
- Rule Tile 오토매핑 — 미착수
- **Map 1-1 ~ 1-12 방 설계** — 32방 그리드는 있으나 콘텐츠가 **복사 기반이라 반복적**
- 함정 3종(`CrumblingPlatform` / `CrumbleDebris` / `PressTrap`) — 코드 구현 + `trap_crumble`/`trap_press`
  PlayTest 시나리오까지 완료(런타임 테스트 오브젝트 방식). 남은 건 **Map1 씬에 실제로 배치하는 것**
- 낙하 파이프 · 고철 덩어리 — 미착수
  (단, `LightOrb.Drop()`을 public 정적 API로 열어두어 나중에 한 줄로 붙일 수 있게 설계됨)

### Week 3 (몬스터 AI) — **전부 미착수**

> 현재 적은 테스트용 `DummyEnemy` 1종뿐이다. **다음 큰 덩어리는 사실상 여기다.**

- 범용 AI 코어: FSM + Vision Cone 시야각 + 장애물 감지
- 소형 부패충 (낭떠러지 감지 순찰)
- 게헨나 포식견 (조준선 → 1초 대기 → 돌진, **패링 타겟**)
- 자폭병 (근접 → 점멸 → CircleCast 폭발)
- 웨이브 아레나 (Map 1-11 철창 낙하 + 스포너 + 전멸 감지)

### Week 4 (보스) · Week 5 (폴리싱) — 미착수

보스 '시체 더미' 페이즈 매니저 · 충격파 · 예측 투사체 · 시네마틱 컷씬 · 융해 셰이더 /
오디오 믹서 · 발소리 피치 랜덤화 · GC Alloc 최적화 · **Windows 스탠드얼론 빌드**

---

## ⚪ 기술 부채 · 정리 대상

| 항목 | 내용 |
| --- | --- |
| `Assets/Shaders/VFXLit2D.shader` | 패스 태그가 `UniversalForward`라 **2D Renderer에서 통째로 스킵**된다 → 사실상 미사용 |
| `Assets/Editor/SetupAnimationsEditor.cs` | 제거된 `TextureImporter.spritesheet`(CS0618)를 아직 사용 |
| `Assets/Editor/SetupInputActions.cs` | 생성기에 `Parry`의 **F키 보조 바인딩이 빠져 있어**, 실행하면 그 바인딩이 사라진다 ⚠️ |
| `PlayerController.OnJump` | `isJumpHeld = false` 분기가 실행되지 않음 (`PlayerInput`이 Button의 `canceled`를 안 보냄). 기존 잠재 버그, 미수정 |
| `.claude/skills/add-combat-move/SKILL.md` | 항목 번호가 중복돼 있다 (2·3·7·8이 두 번씩) — "SKILL 9번" 식 참조가 애매해짐 |
| C-4 / C-5 (블룸 마스킹) | `.shader` 편집 승인이 필요해 보류 중. 부록A(마스크 생성)도 함께 |

---

## ❗ 문서 간 불일치 — 정리 필요

`docs/midterm_presentation.md`(중간 발표 자료)의 서술이 현재 저장소 상태와 맞지 않는다.

| 발표 자료 서술 | 실제 상태 |
| --- | --- |
| "3주차 마일스톤 100% 달성" | Week 2 진행 중, Week 3 미착수 |
| "12개 방 · 웨이브 아레나 구축 완료" | 32방 그리드는 있으나 콘텐츠는 복사 기반, 아레나 없음 |
| "포식견 · 자폭병 등 몬스터 패턴 완료" | `DummyEnemy` 1종만 존재 |
| "부서지는 바닥 · 프레스 함정 완료" | 스크립트만 존재, 씬 배치 미확인 |

> 발표용 문서와 실제 진척을 분리해 관리하거나, 발표 자료를 현재 상태에 맞게 갱신할 것.

---

## 💭 확인이 필요한 설계 판단

1. **맵 콘텐츠의 반복성** — 복사로 채운 방들을 손으로 다시 설계할지, 이 상태로 두고 적 배치로 차별화할지.
2. **벽 콜라이더 배치 규모** — 32방 전체에 다 깔지, 벽타기를 쓰는 구간만 지정할지.
3. **`RoomCamera.cs` 등 삭제 후보** — 지금 정리할지, 커밋 이후로 미룰지.
