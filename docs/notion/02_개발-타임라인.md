# 🗓️ 개발 타임라인

> 2026-07-15 ~ 2026-08-04 · 출처 `task.md`
> 날짜는 작업일 기준. 같은 날 여러 건이면 굵은 항목이 그날의 주 작업.

---

## 🟥 Phase 1 — 기초 다지기 (07-15 ~ 07-20)

| 날짜 | 작업 |
| --- | --- |
| 07-15 | 스프라이트 피봇 17개 일괄 `(0.46, 0.03)` 통일 (**발밑 피봇**) |
| 07-19 | 최초 커밋 · 저장소 개설 |
| — | 이동·물리 기본(가감속 Lerp, 가변 점프, 코요테 0.1s, 벽 슬라이드/점프) |
| — | Animator 재구성: **AnyState → anim → (Exit / next)**, `CanTransitionToSelf` 전부 OFF |
| — | 카메라 방 단위 고정(산나비 스타일) 1차 — `RoomCamera` + `RoomTrigger` |
| — | 이동 즉시화(AddForce → `linearVelocity` 직접 설정) · `moveSpeed` 8 → 5 |
| — | 타일맵 임포트(SuperTiled2Unity) · 솔리드/장식 분리 · **1047/1047 셀 정렬 검증** |
| **07-20** | **무적 대시(I-Frame) 구현** — 레이어 스왑 방식, `PlayerInvincible` 레이어 신설 |
| 07-20 | 루프 모드 승인 흐름 테스트 — git commit이 `NEEDS_APPROVAL`로 정지하는지 확인 ✅ |

---

## 🟧 Phase 2 — 전투의 뼈대 (07-21 ~ 07-24)

| 날짜 | 작업 |
| --- | --- |
| 07-21 | "왼쪽 이동 안 됨" 버그 Play 모드 실측 검증 · 대시 VFX 레퍼런스 조사 |
| **07-22** | **대시 VFX = 산데비스탄 잔상 + Run 프레임 프리즈** 구현 |
| 07-22 | "대시 후 왼쪽 이동 막힘" 3차 시도에서 재현·원인 확정 |
| 07-22 | 낙사 → 리스폰 시스템 구현 → **같은 날 사용자 요청으로 전면 제거** |
| 07-22 | 🎬 **영상 검증 파이프라인 구축** (`TestRecorder` → mp4 → 판정) |
| 07-23 | 대시 잔상 "형태 불일치" 버그 수정 · 1자(플랫) 테스트 타일맵 신설 |
| 07-23 | 공격 튜닝 + 공격 중 입력 제한 + 타격 VFX 이식 |
| **07-23** | **공격 콤보 시스템 재설계** — 입력 버퍼링 + Animation Event 정밀 판정 |
| 07-23 | 데미지 텍스트 UI · 양방향 히트 VFX · 회피-카운터 이식 · 폰트 Silver 적용 |
| **07-23** | **회피(저스트 닷지) 전면 재설계** — 근접판정 → **타이밍 판정** |
| 07-23 | 그레이스케일 확산 VFX · 5개 항목 일괄 수정 · F키 카운터 위치 버그 |
| 07-24 | F키 카운터 후속 버그 3종 + 4종 추가 조정(거리·타이밍·연장·터널링) |
| **07-24** | **F키 "늦게 반응" 진짜 근본 원인** — 윈도우 오픈 **전** 홀드 상태가 무시되고 있었음 |
| 07-24 | 대시 연장 재설계 + Windup 판정 + 잔상 그레이스케일 제외(2차 시도 성공) |
| 07-24 | 버그 5건 일괄 수정 · 공중 공격 금지(F 카운터 제외) |

---

## 🟨 Phase 3 — 필살기와 방어 (07-25 ~ 07-26)

| 날짜 | 작업 |
| --- | --- |
| **07-25** | **일섬(一閃) 구현** — 차지 → 관통 · 궤적 섬광 셰이더 · Hit VFX 정리 |
| **07-25** | **패링 구현** — 짧은 탭 판정 · 실드 · 프리뷰 |
| 07-25 | VFX 배치용 샌드박스 씬 `VfxSandbox.unity` 신설 |
| **07-26** | **처형(Execution) 전면 검토·수정** + 카메라 쉐이크 + FocusPulse 줌 |
| 07-26 | 처형 커서 포커싱을 "블룸" → **실제 붉은 아웃라인**으로 교체 |
| 07-26 | 처형 "Glitch Sweep 마지막 프레임이 길게 남는" 버그 진단·수정 |
| **07-26** | **플레이어 HUD** — 체력바 + 빛 에너지 게이지 (Lerp 스무딩) |
| **07-26** | **체력 = 갯수(칸) 전환 + 데이터 레이어 착수** (`GameData`/`SaveSystem`/`GameDataManager`) |
| 07-26 | ✅ **일정표 1주차 항목 전부 완료** |

---

## 🟩 Phase 4 — 광원 3부작 (08-01 ~ 08-02)

계획서에 없던, 이 프로젝트의 정체성이 된 구간.

| 날짜 | 작업 |
| --- | --- |
| **08-01** | **🔆 광원 시스템 A+C 트랙** — 일섬 게이팅 · 피격 연출 · 획득 픽셀 VFX · E홀드 소모 · 세이브 연동 |
| 08-01 | 버그: 일섬 쿨타임 중 패링 불가 — `CanStartCharge()`가 쿨타임을 막던 것 |
| 08-01 | 1·2차 피드백: 픽셀 산개 · 줌인이 안 보이던 **진짜 버그** 수정 · 일섬을 **채널링형 소모**로 재설계 |
| **08-01** | **🩸 폭주 시야 제한 B-2** — 어둠 렌더러 피처 + 적 실루엣 + 지형 글리치 라인 `PASS (11/11)` |
| 08-01 | 광원 변화량을 **격투게임식 바**로 · **EgoBar 완전 삭제**(자아를 HP 칸에 통합) |
| 08-02 | 드레인 연출 2·3차 수정 → 최종적으로 **기존 "칸 꺼짐" 연출 재활용**으로 단순화 |
| 08-02 | 자아 0일 때 광원도 서서히 감소 |
| **08-02** | **🌌 초월(Transcendence) T-1~T-4 구현** + **4차례 피드백 반영** |
| 08-02 | └ 1차: 지속 5.0 → 12.5초 · 진입 흡수 연출 · 예고 원 두껍게 · 적 Windup 2.5배 |
| 08-02 | └ 2차: 이동·점프·대시 버프 · 판정 완화 · 떠오르는 cyan 픽셀 |
| 08-02 | └ 3차: **적 공격 판정을 원 → 캡슐로 확장**(전체 전투 적용) · 픽셀을 사각 블록으로 |
| 08-02 | └ 4차: 광원 소모 카메라 팬을 **X축만**으로 고정 |
| 08-02 | 초월 진입 순간 연출 B안(가속 릴리즈) · 폭주 이동 애니 버그 · Jump/Run 글리치 스왑 |
| 08-02 | 픽셀 VFX·광원바 **상태별 색상** · 변화량 색상 충돌 해소 |

---

## 🟦 Phase 5 — 레벨과 벽 (08-03)

| 날짜 | 작업 |
| --- | --- |
| 08-03 | 공격 히트박스를 **씬 배치 자식 콜라이더** 기준으로 전환 |
| **08-03** | **실제 타일맵 전환 + 맵 확장(77→174유닛) + Room 기반 카메라 전환 부활** |
| 08-03 | 바닥 좌우 확장 · **Room 크기를 카메라 사이즈 기준으로 재조정**(8×4, 32방) |
| **08-03** | **맵 작업 기준을 SampleScene → `Map1` 씬으로 전환** |
| **08-03** | **벽타기(Wall Climb) 구현** — 기존 Wall Slide 대체 |
| 08-03 | └ 1차 후속: 중력 완전 차단 + 접지 상태에서도 동작 |
| 08-03 | └ 2차: 방향키 유지 불필요, 반대키/Space로만 해제 |
| 08-03 | └ 3차: **벽 꼭대기 자동 오르기**(`TryLedgeClimb`) |
| 08-03 | └ 4차: 애니메이션 버그 2건 (Wall Slide 고착 · 무입력 시 계속 재생) |
| 08-03 | └ 5차: 낮은 턱 걸어 올라가기 + **착지 높이 버그**(피봇이 발밑인 걸 놓침) |
| 08-03 | └ 6차: 공중 회귀 · 벽점프 우선순위 · 벽타기 중 행동 입력 차단 |
| 08-03 | Map1 블룸 미적용 수정 — 원인은 카메라의 `renderPostProcessing = False` |
| 08-03 | 폭주 지형 글리치 라인 조각 버그 — `CompositeCollider2D.GetPath()` 직접 판독으로 교체 |
| 08-03 | 이동·벽타기 애니메이션 속도를 **실제 이동속도**에 연동 |

---

## 🟪 Phase 6 — 시간과 벽의 재정의 (08-04, 최신)

| 날짜 | 작업 |
| --- | --- |
| **08-04** | **⏱️ 시간 가속(Time Accel) 구현** — 전역 `timeScale 0.4` + 플레이어 `×2.5` 보정 |
| 08-04 | └ 회귀 수정: Shift 탭/홀드 충돌로 대시가 안 되던 문제 → **Left Alt 토글**로 분리 |
| 08-04 | └ `dashInputBuffer`(0.12s) 신설 — 대시가 "더 잘 눌러지게" |
| 08-04 | └ 회귀: 가속 중 회피-카운터 불발 — 판정 창은 **세계 시간**으로 재야 함을 실측으로 확정 |
| 08-04 | └ 진입 보너스(+20% 광원) 추가 → **토글 반복으로 무한 획득 가능**해 보고 후 제거 |
| 08-04 | 초월 **아우라 제거** (폭주 2회 + 초월 1회, **총 세 번 거절된 연출**) |
| **08-04** | **벽타기 진입 게이트 근본 수정** — 씬에 `Wall`(10) 콜라이더가 **하나도 없어** 모든 바닥을 벽으로 인식하고 있었음 |
| **08-04** | **벽 지정 방식 전환: 추정 → 명시적 Wall 콜라이더** (`WallColliderTool.cs` 에디터 툴 신설) |
| 08-04 | └ `TryStepUpShortWall` 제거 (플랫폼 순간이동 증상의 정체) |
| 08-04 | └ 낙하 중 벽 잡기 버그(하강 속도가 안 끊겨 3초간 미끄러짐) 수정 |
| 08-04 | └ 벽 꼭대기 오르기를 0.12s 보간으로 (순간이동 느낌 제거) |

---

## 📊 한눈에

| 구분 | 수 |
| --- | --- |
| 기록된 작업 세션 | 약 20일 |
| `task.md` 분량 | **3,766줄** |
| 스크립트 파일 | 46개 (`Assets/Scripts/**`) |
| 커스텀 렌더러 피처 | 3개 |
| 계획 외 추가 대형 시스템 | 5개 (광원 · 폭주 · 자아 · 초월 · 시간 가속) |
